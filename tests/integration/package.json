{
  "name": "@levelup/integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Firebase Emulator integration tests for the LevelUp platform",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:rules": "vitest run --config vitest.config.ts firestore-rules.test.ts",
    "test:flows": "vitest run --config vitest.config.ts auth-flows.test.ts"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^11.2.0",
    "firebase-admin": "^13.3.0",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
